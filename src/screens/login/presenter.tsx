import React from "react";
import {View, Text} from "react-native";

export interface LoginProps {

}

export const LoginPresenter: React.FunctionComponent<LoginProps> = (props) => {

    return (
        <View>
            <Text>Login</Text>
        </View>
    )
}